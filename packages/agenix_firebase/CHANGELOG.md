## 1.0.0
- Initial release — `FirebaseDataStore` extracted from `agenix` core.
- Firestore-backed message persistence with per-user conversation isolation.
- Firebase Storage image upload support.
- Firebase Auth integration with `NotAuthenticatedException` for unauthenticated access.
